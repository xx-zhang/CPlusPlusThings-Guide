# 第 N 课 · `<课程名>`

> **对应**：`A?` `<公理名>` · `T?` `<定理名>`
>
> **耗时**：25~40 分钟
>
> **前置**：`01-知识框架.md` 的 `A?` 与 `T?`；`02-正交轴与术语速查.md` 的`<?>`

---

## 一、先做预测（不要往下翻）

> 这一步比后面读解释重要得多。**先写下你的答案，再看解释。**

给出一段最小代码（只给代码，不给结论），然后问 3 个可被证伪的问题：

1. **预测 1**：这段代码**能不能编译 / 链接通过**？
2. **预测 2**：**运行会输出什么**？`sizeof` / 偏移 / 汇编会是什么？
3. **预测 3**：改动**一处**（指明改哪里），结果会变成什么？

```cpp
// <最小可运行代码>
```

---

## 二、原则

> **<一句话陈述这条公理/定理>**

然后给出**从公理到现象的推导链条**（3~5 步），每步都是必然结果，不是「约定」：

1. 因为 <公理的直接推论> ⟹ ...
2. 所以 <中间结论> ⟹ ...
3. 因此 <本课的现象>。

### <小标题：核心工具 / 判据 / 规则表>

| ... | ... |
| --- | --- |

---

## 三、实测（跑起来，别只看）

> 每个实验都必须给出**可复制的命令**和**真实输出**。

### 实验 1 —— `<这个实验要证明什么>`

```bash
<命令>
```

**预期看到**：

```text
<真实输出>
```

**结论**：<这个输出证明了什么，一句话>

### 实验 2 —— `<这个实验要证伪什么>`

```bash
<命令>
```

**预期看到**：

```text
<真实输出>
```

**结论**：<一句话。如果是证伪，明确写出「仓库的说法在此不成立」>

### 实验 3 —— 关键边界（这一步最容易漏）

```bash
<命令>
```

**预期看到**：

```text
<真实输出>
```

**结论**：<说出这条规则的**边界条件**>

---

## 四、这一课在 AI 生成的代码里对应的坑

> 每个坑按 **「长什么样 → 为什么 AI 会犯 → 修法」** 三段式写。

### 坑 1 · `<症状>`

**长什么样**：

```cpp
// ❌
```

**为什么 AI 会犯**：<一句话解释它的心智模型错在哪>

**修法**：

```cpp
// ✅
```

### 坑 2 · `<症状>`

同样的三段式。

---

## 五、自测题（先自己答，再翻答案）

**题目**：<代码 + 问题>

**Q1**：...
**Q2**：...
**Q3**：...

---

## 六、答案

### Q1

<答案 + 依据（哪条公理/定理）+ 验证命令>

### Q2

<答案 + 依据>

### Q3

<答案 + 依据 + 如果这里存在常见误解，明确指出>

---

## 七、归档（按 `03-学习方法与步骤.md` 的第六步）

请把下面这段抄进你自己的笔记，并把最后一行的反例**亲手写出来**：

```markdown
## 主题：<名字>                                [对应：A? / T?]

- 原则：<一句话>
- 前提：<这条原则在什么条件下不成立 —— 最重要的一行>
- 反例：<你自己构造的、仓库里没有的失败案例>
```

---

## 八、下一课

**第 N+1 课 · `<名字>`**（见 `04-学习路线图.md`）

**准备**：<下一课开始前要先掌握什么>

---

## 附 · 本课用到的命令速查

```bash
<只列本课真正用到的命令，并注明每条的用途>
```

---

<!--
写作检查清单（写完本课后自查，确认后删除本注释）：

- [ ] 第一节的预测题是**可被证伪**的（有唯一答案），不是「你觉得呢」
- [ ] 第二节的推导链条每一步都是「必然结果」，没有出现「规定」「约定」
- [ ] 第三节每个实验都给了**真实输出**，不是「应该会输出」
- [ ] 第三节至少有一个实验是**证伪**或**探边界**的
- [ ] 第四节的坑是**真实高频**的，不是我编的
- [ ] 第五节的题目覆盖「判定 / 预测 / 构造」三层
- [ ] 第六节的答案都注明了**依据哪条公理/定理**
- [ ] 全文没有裸的「**小标题**」独占一行（会触发 markdownlint MD036）
- [ ] 所有纯文本代码块都标了 `text` 语言（避免 MD040）
- [ ] 涉及布局/汇编的结论都标注了「ABI 相关，换平台需重测」
-->
